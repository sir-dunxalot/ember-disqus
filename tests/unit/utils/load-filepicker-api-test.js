import loadFilepickerApi from '../../../utils/load-filepicker-api';
import { module, test } from 'qunit';

module('Unit | Utility | load filepicker api');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = loadFilepickerApi();
  assert.ok(result);
});
